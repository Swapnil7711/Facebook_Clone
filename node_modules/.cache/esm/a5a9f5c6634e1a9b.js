let Joi,User,CustomeErrorHandler,bcrypt,jsonToken,refresh_tokenSecret,RefreshToken;_634‍.x([["default",()=>_634‍.o]]);_634‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_634‍.w("../models",[["User",["User"],function(v){User=v}],["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}]]);_634‍.w("../services/CustomErrorHandler",[["default",["CustomeErrorHandler"],function(v){CustomeErrorHandler=v}]]);_634‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_634‍.w("../services/jwtToken",[["default",["jsonToken"],function(v){jsonToken=v}]]);_634‍.w("../config",[["refresh_tokenSecret",["refresh_tokenSecret"],function(v){refresh_tokenSecret=v}]]);







const loginController = {

    async login(req, res, next) {


        // validate the inputs
        _634‍.g.console.log(req.body)

        const loginShema = Joi.object({
            email: Joi.string().email().required(),
            password: Joi.string().pattern(/^[a-zA-Z0-9]{3,30}$/).required()
        })

        const { error } = loginShema.validate(req.body)

        if (error) {
            next(error)
        }

        // check if user exsists in database for credentials

        const { email, password } = req.body

        try {

            var user = await User.findOne({ email: email })

        } catch (error) {
            _634‍.g.console.log(error)
        }


        _634‍.g.console.log(user)

        if (!user) {
            return next(CustomeErrorHandler.wrongCredentials())
        }

        // compare password
        console.log(`${user.password} ${req.body.password}`)
        const match = await bcrypt.compare(password, user.password)
        _634‍.g.console.log(match)

        if (!match) {
            return next(CustomeErrorHandler.wrongCredentials())
        }

        const access_token = jsonToken.sign({ email: user.email, name: user.first_name, _id: user._id })
        const refresh_token = jsonToken.sign({ email: user.email, name: user.first_name, _id: user._id }, refresh_tokenSecret, '1y')

        await RefreshToken.create({ token: refresh_token })
        res.json({ access_token: access_token, refresh_token: refresh_token })
    },

    async logout(req, res, next) {

        // check if the request has th e token and delete it from server

        const refreshTokenSchema = Joi.object({
            token: Joi.string().required()

        })
        _634‍.g.console.log("reqbody", req.body)
        const { error } = refreshTokenSchema.validate(req.body)

        if (error) {
            return next(error)
        }

        try {

            const deleted = await RefreshToken.deleteOne({ token: req.body.token })
            _634‍.g.console.log("deleted", deleted)
            if (deleted.deletedCount < 1) {
                return next(CustomeErrorHandler.unAuthorized("not deleted"))
            }

        } catch (error) {

            return next(new Error("something went wrong!!"))

        }

        res.json({ logout: true })

    }

}


_634‍.d(loginController);

